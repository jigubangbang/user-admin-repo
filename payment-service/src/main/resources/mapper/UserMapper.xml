<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jigubangbang.payment_service.mapper.UserMapper">

    <select id="findById" resultType="com.jigubangbang.payment_service.model.UserResponseDto">
        SELECT
            user_id AS userId,
            email,
            nickname,
            customer_uid AS customerUid,
            is_premium AS isPremium
        FROM user
        WHERE user_id = #{userId}
    </select>

    <update id="updateUserPremium">
        UPDATE user
        SET
            is_premium = #{request.isPremium},
            customer_uid = #{request.customerUid}
        WHERE user_id = #{userId}
    </update>

</mapper>
